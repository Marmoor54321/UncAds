@model UncAds.Models.Ad
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer

@{
    // Używamy zlokalizowanego tekstu jako tytułu strony
    ViewData["Title"] = Localizer["DeleteAdTitle"];
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">

            <div class="card border-danger shadow">
                <div class="card-header bg-danger text-white text-center py-3">
                    <h3 class="m-0"><i class="bi bi-exclamation-triangle-fill"></i> @Localizer["ConfirmDeletionHeader"]</h3>
                </div>
                <div class="card-body p-4 text-center">

                    <h5 class="card-title text-danger mb-3">@Localizer["ConfirmationQuestion"]</h5>
                    <p class="text-muted mb-4">@Localizer["IrreversibleWarning"]</p>

                    <div class="bg-light p-3 rounded border mb-4 text-start">
                        <dl class="row mb-0">
                            <dt class="col-sm-4 text-secondary">@Localizer["TitleLabel"]:</dt>
                            <dd class="col-sm-8 fw-bold text-dark">@Html.DisplayFor(model => model.Title)</dd>

                            <dt class="col-sm-4 text-secondary">@Localizer["DateAddedLabel"]:</dt>
                            <dd class="col-sm-8">@Html.DisplayFor(model => model.Date)</dd>

                            <dt class="col-sm-4 text-secondary">@Localizer["CategoriesLabel"]:</dt>
                            <dd class="col-sm-8">
                                @if (Model.AdCategories != null && Model.AdCategories.Any())
                                {
                                    @foreach (var ac in Model.AdCategories)
                                    {
                                        <span class="badge bg-secondary me-1">@ac.Category.Name</span>
                                    }
                                }
                                else
                                {
                                    <span>-</span>
                                }
                            </dd>
                        </dl>
                    </div>

                    <form asp-action="Delete" class="d-flex justify-content-center gap-3">
                        <input type="hidden" asp-for="Id" />
                        <a asp-action="Index" class="btn btn-outline-secondary px-4">@Localizer["CancelButton"]</a>
                        <button type="submit" class="btn btn-danger px-4 fw-bold">@Localizer["ConfirmDeleteButton"]</button>
                    </form>
                </div>
            </div>

        </div>
    </div>
</div>