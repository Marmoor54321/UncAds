@model IEnumerable<UncAds.Models.CategoryAttribute>
@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer

@{
    ViewData["Title"] = Localizer["IndexTitle"];
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="m-0"><i class="bi bi-list-check"></i> @Localizer["Header"]</h2>
        <a asp-action="Create" class="btn btn-success rounded-pill px-4 shadow-sm">
            <i class="bi bi-plus-lg"></i> @Localizer["AddAttributeButton"]
        </a>
    </div>

    <div class="card border-0 shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light">
                        <tr>
                            <th class="ps-4">@Localizer["Table_Name"]</th>
                            <th>@Localizer["Table_FieldType"]</th>
                            <th>@Localizer["Table_Configuration"]</th>
                            <th>@Localizer["Table_Category"]</th>
                            <th class="text-end pe-4">@Localizer["Table_Actions"]</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="ps-4 fw-bold">@item.Name</td>
                                <td>
                                    <span class="badge bg-info text-dark">@item.Type</span>
                                </td>
                                <td>
                                    @if (!string.IsNullOrEmpty(item.Options))
                                    {
                                        <small class="text-muted" title="@item.Options">
                                            <i class="bi bi-ui-checks"></i> @Localizer["Config_OptionsDefined"]
                                        </small>
                                    }
                                    else if (item.DictionaryId.HasValue)
                                    {
                                        <small class="text-primary">
                                            <i class="bi bi-book"></i> @Localizer["Config_Dictionary"]
                                        </small>
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </td>
                                <td>
                                    <span class="badge bg-light text-dark border">
                                        <i class="bi bi-folder"></i> @item.Category.Name
                                    </span>
                                </td>
                                <td class="text-end pe-4">
                                    <div class="btn-group btn-group-sm">
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-primary" title="@Localizer["Action_Edit"]">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-info" title="@Localizer["Action_Details"]">
                                            <i class="bi bi-info-circle"></i>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger" title="@Localizer["Action_Delete"]">
                                            <i class="bi bi-trash"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>